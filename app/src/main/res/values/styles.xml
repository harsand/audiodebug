<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>
    
    <style name="RingtoneDialogTheme" parent="@android:style/Theme.Dialog">
        <!-- 控件背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>  
        <!--  -->
        <item name="android:windowFrame">@null</item> 
        <!--  --> 
        <item name="android:windowNoTitle">true</item>  
        <!--  -->
        <item name="android:windowIsFloating">true</item> 
        <!--  --> 
        <item name="android:windowIsTranslucent">true</item> 
        <!--  --> 
        <item name="android:windowContentOverlay">@null</item>  
        <!--  -->
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>  
        <!--  -->
        <item name="android:backgroundDimEnabled">true</item>   
    </style>
    
    <style name="RecordListDialogTheme" parent="@android:style/Theme.Dialog">
        <!-- 控件背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>  
        <!-- Dialog的windowFrame框为无 -->
        <item name="android:windowFrame">@null</item> 
        <!-- 是否显示title --> 
        <item name="android:windowNoTitle">true</item>  
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item> 
        <!-- 窗体是否半透明 --> 
        <item name="android:windowIsTranslucent">true</item> 
        <!-- <item name="android:windowContentOverlay">@null</item>  设置窗体内容背景 --> 
        <item name="android:windowFullscreen">true</item>
        <!--  <item name="android:windowShowWallpaper">false</item>  是否显示壁纸 -->
        <!--  <item name="android:windowFullscreen">false</item>   是否为全屏 -->
        <!--  <item name="android:windowOverscan">false</item> 是否要求窗体铺满整屏幕 -->
        <!-- <item name="android:windowCloseOnTouchOutside">false</item> 是否再点击外部可关闭 -->
        <!-- <item name="android:windowContentOverlay">@null</item>  自定义Titlebar时去掉多余的阴影 -->
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>  
        <!-- 背景是否模糊显示 -->
        <item name="android:backgroundDimEnabled">true</item>   
    </style>
    
</resources>
